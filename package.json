{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.30.1",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run build:vihsafe",
    "build:vihsafe": "pnpm run typecheck:vihsafe && pnpm --filter @workspace/vihsafe run build",
    "typecheck:libs": "tsc --build",
    "typecheck:vihsafe": "pnpm run typecheck:libs && pnpm --filter @workspace/vihsafe run typecheck",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
